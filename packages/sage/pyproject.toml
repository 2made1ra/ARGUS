[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sage"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pymupdf",
    "pytesseract",
    "pillow",
    "httpx",
    "pydantic",
]

[tool.hatch.build.targets.wheel]
packages = ["sage"]
